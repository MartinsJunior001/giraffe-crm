# Board operacional

Fotografia do estado operacional. **Escrito exclusivamente pela Lane 0** — Writers e QA leem, não editam.

**Não é fonte de verdade.** A ordem de reconciliação está no `CLAUDE.md` (§ Protocolo Autônomo de Aceleração → Fonte de verdade): `origin/main` → PRs e CI → `sprint-status` → BMAD/Spec Kit → branches e worktrees → **board** → MEMORY. Divergiu do repositório, o errado é o board.

Estados: `backlog` · `assigned` · `in-progress` · `pr-open` · `in-review` · `ready-to-merge` · `merged` · `closed` · `blocked`.

## Stories em voo

| Story | Estado | Writer | QA | Branch / worktree | PR | CI | Bloqueio | Próxima ação | Prio |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 4.7 — Encadeamento e prevenção de ciclos | `closed` | Subagente Writer (Lane 0) | Lane 0 (QA + Security cruzados) | `story/4-7-...` (mergeado) | #167 → main `4769a46` | 5/5 | — | **DONE.** Prevenção de ciclos: assinatura de visita determinística + freio de profundidade (imutável por GRANT, checado antes de `ehRaiz`) + duração-de-cadeia; dead-letter `HALTED_BY_LIMIT` não-reivindicável. QA + Security 0 BLOCKER/0 HIGH; MEDIUM de docstring corrigido docs-only (`01ccba2`). | P0 |
| 4.8 — Trilha de execuções (read-side) | `closed` | Writer subagente (Lane 0) | Lane 0 (QA + Security cruzados) | `story/4-8-...` (mergeado) | #169 → main `7af9aa0` | 10/10 | — | **DONE.** Aba "Execuções" read-side puro sobre `AutomationExecution`/`AutomationActionResult` + cadeia; sanitização allowlist AD-30 (asserção negativa), autz piso=operar + teto GUEST, Membro restrito só vê recursos que acessa, 404 não-enumerante, filtros fail-closed + cursor determinístico. QA (42/42 PG real) + Security 0 BLOCKER/HIGH/MEDIUM; LOW de Security (filtro nos códigos raw) fechado em `5dc86b7`. | P0 |
| 4.9 — Contrato de extensão de Ações e referências | `closed` | Writer subagente (Lane 0) | Lane 0 (QA + Security cruzados) | `story/4-9-...` (mergeado) | #171 → main `84e7a9a` | 10/10 | — | **DONE — ÚLTIMA do Épico 4.** Contrato tipado `HandlerDeAcao` (11 facetas do §1459) derivado do catálogo 4.5 (fonte única); registro FECHADO dos 8 handlers núcleo + pontos de extensão E5/E6 sem executor (recusados na config, fail-closed `ACAO_DE_EXTENSAO_INDISPONIVEL`); consumo-por-construção dos identificadores de Evento pelo motor (sem drift). Sem migration/GRANT/schema; C3 congelado. Template/IA registrados como contrato-futuro (DEB-4-9-*). QA + Security independentes 0 BLOCKER/HIGH/MEDIUM. | P0 |
| 5.1 — Tarefas: ciclo de vida e acompanhamento | `closed` | Writer subagente (Lane 0) | Lane 0 (Arch + QA + Security cruzados) | `story/5-1-...` (mergeado) | #173 → main `c5c7ede` | 5/5 | — | **DONE — 1ª do Épico 5.** Entidade `Task`/`TaskHistory`/`TaskOverdueOccurrence` (RLS FORCE+WITH CHECK, GRANT column-scoped sem DELETE, append-only). Ciclo de vida aberta/concluída + arquivamento; `atrasada` derivado; mecanismo temporal Postgres-based idempotente (`@@unique([orgId,taskId,dueVersion])`); Responsável=Membership ativa + reatribuição E8; anexos via 3.7/3.8 (branch TASK). 3 revisores independentes 0 BLOCKER/HIGH (F1 anexos corrigido em `bcdff93`, reancorado). | P0 |
| 5.2 — Solicitações: ciclo de vida e Responsável | `closed` | Writer subagente (Lane 0) | Lane 0 (Arch + QA + Security cruzados) | `story/5-2-...` (mergeado) | #175 → main `a851040` | 10/10 | — | **DONE.** Twin da 5.1 SEM eixo temporal (estados `aberta`/`resolvida`; sem prazo/`atrasada`/scheduler). Entidade `Solicitacao`/`SolicitacaoHistory` (RLS FORCE+WITH CHECK, GRANT column-scoped sem DELETE, append-only). Responsável 0..1 + reatribuição E8 (2ª extensão de `membership-contract`, aditiva); anexos via 3.7/3.8 (branch `SOLICITACAO` no dispatcher — **entregues no wiring inicial**, lição da 5.1). 3 revisores independentes 0 BLOCKER/HIGH/MEDIUM; red-phase provada; regressão E8/5.1 verde. | P0 |
| 5.3 — Fonte única de Notificações (write-side) | `closed` | Writer subagente (Lane 0) | Lane 0 (Arch + QA + Security cruzados) | `story/5-3-...` (mergeado) | #177 → main `93fdeb4` | 10/10 | — | **DONE.** Modelo canônico INV-NOTIF-01: `Notification` (append-only imutável, `@@unique(orgId,sourceEventId,type)`) + `NotificationRecipient` (mutável, `readAt` **derivado**, UPDATE column-scoped, `@@unique(orgId,dedupeKey)`); `NotificationsService` (escrita idempotente via `createMany skipDuplicates`) + núcleo puro de sanitização (anti-XSS/prototype-pollution/allowlist fail-closed) + `marcarComoLida`. **Write-side puro** (sem rota/controller — rota é 5.4). 3 revisores 0 BLOCKER/HIGH; 2 fixes pré-merge (higiene: bytes de controle→escapes; MEDIUM: precisão `TIMESTAMPTZ(3)`; LOW: auditoria idempotente-no-op). | P0 |
| 5.4 — Superfícies, leitura e preferências | `closed` | Writer subagente (Lane 0) | Lane 0 (Arch + QA + Security cruzados) | `story/5-4-...` (mergeado) | #179 → main `9a7c183` | 5/5 | — | **DONE.** Read-side (badge/popover/página) sobre a fonte 5.3 + contagem no servidor (janela CAP=100 + "99+") + marcar-lida/marcar-todas (cursor de corte, `recipientMembershipId` autenticado) + preferências por tipo (`NotificationPreference` org+usuário+tipo, RLS+GRANT column-scoped sem DELETE). **Revalidação-de-acesso na leitura** via dispatcher por `resourceType` (CARD/TASK/SOLIC→pipe-authz, RECORD→database-authz, desconhecido→deny) — oculta e exclui-de-contagem o inacessível, sem N+1 (batch+memoização); fase vermelha (revoke→oculta) em PG real. 3 revisores 0 BLOCKER/HIGH. | P0 |
| 5.5 — Tempo real como invalidação (Socket.IO) | `closed` | Writer subagente (Lane 0) | Lane 0 (Arch + QA + Security cruzados) | `story/5-5-...` (mergeado) | #181 → main `827a608` | 5/5 | — | **DONE.** Socket.IO (deps fixadas via context7-check: `@nestjs/websockets@11.1.28`, `socket.io@4.8.3`) como **invalidação, não fonte de verdade**: gateway `notifications/realtime/` — handshake reusa better-auth + Membership ativa (deny-by-default, re-auth na reconexão), sala `(userId,orgId)` server-derived, payload só `{id,at}` sanitizado, revogação nos 3 sítios (suspend/remove/troca-de-Org, backstop 5.4), backpressure (coalescing + teto de sockets), degradação graciosa (app funciona sem socket), CORS sem wildcard. Consumidor = hook no `registrarNotificacao` (5.3). Sem migration/GRANT; C3 congelado. 3 revisores 0 BLOCKER/HIGH. | P0 |
| 5.6 — Catálogo e distribuição de Notificações in-app | `closed` | Writer subagente (Lane 0) | Lane 0 (Arch + QA + Security cruzados) | `story/5-6-...` (mergeado) | #183 → main `e008f7f` | 10/10 | — | **DONE.** Catálogo canônico (`notification-catalog.ts`, 5 tipos E5 + 2 slots E6/E8 fail-closed) + distribuidor (`notifications/distribution/`) que resolve destinatários por tipo, só Membership ATIVA + acesso ATUAL (reusa revalidação 5.4/2.10 via `resolverPoderDaMembershipNoPipe`), dedup (múltiplos papéis→1 via `dedupeKey`), preferências ANTES da entrega, ausência→resultado explícito, ninguém fora da Org, ator por regra. Usa a fonte 5.3 (`registrarNotificacaoNoContexto`, idempotente). Gate OQ-33 fechado; **sem migration/GRANT**; C3 congelado. Fecha `DEB-5.4-TIPO-OBRIGATORIO`. 3 revisores 0 BLOCKER/HIGH. | P0 |
| 5.7 — Integração com Automação (Eventos/Ações de E5) | `assigned` | Writer (Lane 0) — a iniciar | Lane 0 (Arch + QA + Security cruzados) | a criar | — | — | — | **ÚLTIMA do Épico 5.** Integra E5 ao motor E4 (4.9 `DEB-4-9-E5-E6-HANDLER-REGISTRO`): registra os **Eventos de E5** (Tarefa/Solicitação) como gatilhos e as **Ações de E5** (criar Tarefa/Solicitação, notificar) no contrato de extensão do motor 4.9 — sem motor paralelo. Consome `DEB-5.6-CARD-MOVED-AUTOMATION-WIRING` (trigger motor→notificação). Deps: 5.1-5.6, E4 (4.9). | P0 |
| 8.2 — Convite: criar/reenviar/cancelar | `merged` | Terminal B | Terminal A | `story/8-2-...` | #132 | 5/5 | **EXTERNAL_GATE** (aceite externo) Resend — domínio `giraffedev.cloud` VERIFIED; `APP_PUBLIC_URL` recriada correta (`https://giraffe-crm-staging.2.24.77.65.sslip.io`, **sem** `:3000`) + redeploy | **aceite externo** pendente (2 provas humanas): (1) `APP_PUBLIC_URL` dentro do container; (2) smoke real criar→enviar→receber→aceitar→ativar membership→bloquear reuso do token→auditoria. Depois: closure → done. **NÃO DONE.** | P0 |

**✅ ÉPICO 4 (Automações internas) COMPLETO — 9/9 Stories done.** `epic-4: done`. Todas integradas no `main` com CI serial verde e QA + Security independentes 0 BLOCKER/0 HIGH: 4.1 (modelo/escopo) · 4.2 (ciclo de vida) · 4.3 (catálogo de Eventos) · 4.4 (catálogo de Condições/AND fail-closed) · 4.5 (catálogo de Ações + PrincipalAutomação) · 4.6 (motor de disparo/ledger/lease) · 4.7 (encadeamento/prevenção de ciclos) · 4.8 (trilha de Execuções read-side) · 4.9 (contrato de extensão de Ações). Último merge: **`84e7a9a`** (PR #171). **Contratos entre Stories:** o motor 4.6 consome o catálogo de Eventos 4.3 + Condições 4.4 + Ações 4.5; a prevenção de ciclos 4.7 herda `chainDepth`/`executionChainId` do envelope 4.3; a trilha 4.8 lê os ledgers 4.6/4.7; o contrato 4.9 formaliza os handlers 4.5/4.6 e é o ponto de extensão de E5/E6. **Débitos de contrato-futuro (AD-11, nenhum bloqueia):** DEB-4-6-DRIVER-CONTINUO (driver contínuo do drain), DEB-4-7-ACTION-EXEC-TIMEOUT-HARD (fio dos timeouts lógicos), DEB-4-7-REPROCESSAMENTO, DEB-4-8-CONDICOES-POR-CONDICAO, DEB-4-9-E5-E6-HANDLER-REGISTRO, DEB-4-9-TEMPLATE-VERSION-RATIFY (ratificar snapshot-na-execução antes de E6), DEB-4-9-IA-COMMAND-STATE. **M2** (`DEB-GUEST-CEILING-CARDGRANT`) segue como decisão de Produto pendente do dono (não bloqueia). Detalhe histórico por Story abaixo. **4.8** (Trilha de Execuções) integrada no main `7af9aa0` (PR #169): módulo read-side `pipes/automations/executions/` (núcleo puro `execution-view.ts` + `executions-read.service` + controller/dto) — aba "Execuções" sanitizada e autorizada sobre `AutomationExecution`/`AutomationActionResult` + metadados de cadeia (4.7). **Read-side PURO** (sem migration/GRANT/schema; C3 congelado): sanitização allowlist AD-30 (`codigoSanitizado` fail-closed nos códigos crus e derivados; asserção negativa), autz piso=`exigirOperarPipe` + teto GUEST fail-closed (Viewer/Convidado 403, sem acesso 404 não-enumerante), Membro `restritoAoProprio` só vê Execuções de recursos que acessa (Responsável ∪ `CardGrant`, filtro no `where` antes da paginação — sem vazar `total`/existência), mascaramento fail-closed de `targetResourceId` inacessível, isolamento cross-tenant por RLS, filtros por período/estado/Evento fail-closed (→400) + cursor determinístico `[createdAt,id]`. **QA (42/42 PG real) + Security independentes 0 BLOCKER/0 HIGH/0 MEDIUM**; LOW de Security (gap doc↔código: filtro estrutural nos códigos raw) fechado em `5dc86b7`, reancorado. **Débitos novos:** `DEB-4-8-CONDICOES-POR-CONDICAO` (4.4/4.6 não persistem resultado por-Condição; expõe agregado honesto), `DEB-4-8-CHAIN-TREE`, `DEB-4-8-TARGET-CROSS-DOMAIN`, `DEB-4-8-INDEX-LISTAGEM`. **4.7** (Encadeamento e prevenção de ciclos) integrada no main `4769a46` (PR #167): módulo `engine/chain-guard.core.ts` (núcleo puro) + migration `20260729120000_automation_chaining` — `AutomationChainVisit` append-only (RLS FORCE + WITH CHECK + GRANT SELECT/INSERT + `@@unique(orgId, executionChainId, signature)`), `chainDepth` INSERT-only em `AutomationExecution`+`DomainEvent`, enum `HALTED_BY_LIMIT`. Terminação garantida pelo **freio de profundidade** (incondicional, imutável por GRANT, não-forjável) + **assinatura de visita determinística** (ciclo A→A/A→B→A barrado) + **duração-de-cadeia** (5 min); dead-letter não-reivindicável; fail-closed. **QA + Security independentes 0 BLOCKER/0 HIGH** (Security 0 MEDIUM; provas a–i contra PG16 real). MEDIUM do QA (docstring falso dos timeouts lógicos) corrigido **docs-only** em `01ccba2` — sem tocar lógica. **Débitos novos:** `DEB-4-7-ACTION-EXEC-TIMEOUT-HARD` (fio dos timeouts lógicos no motor), `DEB-4-7-ENVELOPE-DEPTH-FAIL-CLOSED`, `DEB-4-7-REPROCESSAMENTO`, `DEB-4-7-CHILD-EVENT-SWEEP`. Consome parcialmente `DEB-4-6-DRIVER-CONTINUO`. **4.6** (Motor de disparo e avaliação) integrada no main `4bf24c9` (PR #164): módulo `engine/` — outbox→drain→executors, ledger `AutomationExecution` (dedup por Execução) + `AutomationActionResult` (append-only, dedup por Ação), claim `FOR UPDATE SKIP LOCKED`, recuperação por lease, retry/backoff; snapshot builder sob RLS (fecha DEB-4-4, aplica **M-1** com guarda simétrica); executores reais (3 Ações executam com SC-2101/2102; 5 sensíveis `BLOCKED_CONFIRMATION` por L-1); migration com GRANT column-scoped + append-only + FK composta. **QA + Security independentes 0 BLOCKER/HIGH** (não-ampliação provada; dedup em 2 camadas; provas de integração de SC-2101/lease-recovery/confirmação adicionadas por exigência da Lane 0 — Constituição X). Fecha DEB-4-4-SNAPSHOT-BUILDER, DEB-4-5-EVENTO-ALVO-CONTAINMENT, DEB-4-5-MEMBERSHIP-REF. **Débitos novos:** DEB-4-6-DRIVER-CONTINUO (loop contínuo/dead-letter → 4.7), DEB-4-6-SNAPSHOT-LIVE-STATE, DEB-4-6-CLAIM-AUDIT, DEB-4-6-DENIED-NOISE, DEB-4-6-CONFIRMACAO-CONTINUACAO. Gate **DEB-PIPEGRANT-GUEST-CEILING** fechado; **M2** (`DEB-GUEST-CEILING-CARDGRANT`, decisão de Produto pendente) e **DEB-4-4-DATETIME-TZ** não bloqueiam. **CVE `sharp`** corrigido (#163).

**Épico 8 — Implementação CONCLUÍDA; aceite externo da Story 8.2 pendente.** (Classificação oficial do dono — NÃO "implementação parcial": todo o código está no `main` verde; `epic-8: in-progress` reflete só o aceite externo pendente.) Encerradas: **8.1 · 8.3 · 8.4 · 8.5 · 8.6 · 8.7 · 8.8** (+ dependência **1.12**), todas com QA cruzado 0 BLOCKER/HIGH. Última integrada: **8.8** (PR #150, main `ec42225`, auditoria read-side sobre `MembershipEvent`; projeção allowlist AD-30 fail-closed; `AUDIT_LOG_VIEWED` sem copiar resultados; sem migration; retenção 24m = gate de PRODUÇÃO deferido). **Único item pendente do Épico 8 = aceite externo da 8.2 (Resend)** — 2 provas humanas (`APP_PUBLIC_URL` no container + smoke real completo); variável recriada + redeploy (não repetir sem nova falha). `epic-8` fica `in-progress` até o aceite externo da 8.2.

**Épico 5 (Tarefas, Solicitações e Notificações) — EM PROGRESSO (`epic-5: in-progress`).** 6/7: **5.1**–**5.5** + **5.6** (catálogo/distribuição, `e008f7f`, #183) done. Resta só **5.7** (integração com Automação — última do E5; conecta os Eventos/Ações de E5 ao motor E4/contrato 4.9). Débitos da 5.6: `DEB-5.6-CARD-MOVED-AUTOMATION-WIRING` (trigger na 5.7), `DEB-5.6-CAP-ANTES-REVALIDACAO` (cap sobre candidatos brutos antes da revalidação por-candidato — exposição baixa Fase 1), `DEB-5.6-OVERDUE-AT-LEAST-ONCE` (outbox durável p/ TASK_OVERDUE). **Débitos novos da 5.5 (não-bloqueantes, follow-up):** `DEB-5.5-PORT-LAYERING-KERNEL` (mover a port de tempo real para `kernel/realtime/` — inversão de dependência limpa, recomendação do Arch, trivial), `DEB-5.5-CROSSORG-SOCKET-TEST` (teste de socket dedicado p/ forja cross-Org — comportamento já seguro por inspeção/HTTP), `DEB-5.5-THROTTLE-MEMORY` (limpeza de `ultimoPorSala` em processo longevo), `DEB-5.5-WS-UPGRADE-STAGING` (validar upgrade WS atrás do Coolify/Traefik no smoke de staging), `DEB-5.5-REALTIME-MULTINODE` (Redis adapter p/ multi-réplica; MVP single-instance com backstop 5.4). (leitura/preferências 5.4, tempo-real Socket.IO 5.5, distribuição in-app 5.6, integração com Automação 5.7). **Débitos do E5:** `DEB-5-1-OVERDUE-DRIVER`, `DEB-5-3-SANITIZE-TETO-ORDEM` (LOW cosmético, sem risco XSS — teto aplicado após escape), `DEB-SCHEMA-DRIFT-PREEXISTENTE` (alheio ao E5). A partir da 5.3 muda o eixo (Notificações — INV-NOTIF-01 write-side; depois leitura/preferências 5.4, tempo-real Socket.IO 5.5, distribuição in-app 5.6, integração com Automação 5.7). Ordem autoritativa epics §252 (E5 precede E4; E5 é o consumidor de `DEB-4-9-E5-E6-HANDLER-REGISTRO`). É **autônomo/in-app** (sem Resend). Restam 5.2–5.7. **Débito novo (achado na re-Arch da 5.1, PRÉ-EXISTENTE, alheio à 5.1):** `DEB-SCHEMA-DRIFT-PREEXISTENTE` — `prisma migrate diff` contra o banco propõe `DROP INDEX InviteRoute_orgId_idx`/`LoginFailure_windowStart_idx` (não-parciais, de E8/auth) + `RenameIndex` em `AutomationExecution` (truncamento de identificador 63 chars, de E4). Não afeta runtime, mas um `migrate dev` futuro tentaria reconciliar — abrir tech-debt de alinhamento schema↔migrations.

## Reservas ativas (anticolisão)

Superfícies com Writer exclusivo enquanto a Story estiver em voo. Quem não é o dono não edita.

| Superfície | Reservada por |
| --- | --- |
| `apps/api/src/organizations/invites/` (write-side) | Terminal B (8.2, EXTERNAL_GATE — congelado até o smoke) |

Nenhuma Story de implementação em voo — o Épico 8 está code-complete. Todas as superfícies de `organizations/` (members, roster, audit) estão integradas no main. `invites/` segue congelado só para o smoke da 8.2.

## Fila de integração

Nenhuma integração de código pendente. Resta:

1. **Smoke externo da 8.2** (Resend: API key + domínio verificado + `APP_PUBLIC_URL` HTTPS no Coolify — gate humano) → closure 8-2→done → `epic-8` fecha.
2. Gate de PRODUÇÃO da auditoria (D-4): retenção 24m/legal-hold/anonimização/backups com Governança/Jurídico (`DEB-8-8-RETENCAO-PRODUCAO`) — não bloqueia staging, bloqueia produção.
3. `epic-8-retrospective` (optional).

---
scope: Giraffe CRM Fase 1 - arquitetura (spine de invariantes)
purpose: Converter PRD+UX+stack em invariantes AD-n para orientar Spec/Epicos/Stories, sem codigo/schema/migrations
altitude: initiative (Fase 1 completa)
updated: 2026-07-17T12:10
---

- (decision) A1/AD-cand: Paradigma = monolito modular (NestJS modular por dominio). Binds: todo backend. Prevents: erosao de fronteiras entre dominios (Pipe!=Database, Card!=Registro, Plataforma!=Organizacao). Rule: fronteiras de dominio sao invariante, nao convencao.
- (decision) A1/AD-cand: Topologia = Next.js (frontend) e NestJS (backend) como aplicacoes SEPARADAS, deploy em containers DISTINTOS via Coolify/Docker Compose. Rule: nenhuma regra de negocio de dominio vive no frontend; frontend consome apenas a API interna do NestJS.
- (decision) A1/AD-cand: Monorepo, mas compartilhar APENAS contratos publicos internos, schemas de validacao e tipos utilitarios entre front e back. Prevents: acoplamento do frontend a entidades de dominio/modelos do ORM/detalhes internos do backend. Rule: entidades de dominio, modelos de ORM e internals do backend NUNCA sao expostos ao frontend.
- (decision) A1/AD-cand: Kernel minimo = so capacidades transversais (identidade e sessao, contexto de Organizacao, autorizacao, observabilidade, abstracoes comuns realmente necessarias). Prevents: kernel virar modulo generico com regra de negocio. Rule: regra de negocio vive nos dominios, nunca no kernel.
- (decision) A1/AD-cand: Separacao conceitual de identidade -> Conta/Identidade = escopo Plataforma; Membership/Participacao = vinculo usuario<->Organizacao; Perfil organizacional e papel = escopo da Organizacao atual.
- (constraint) A1: Modulo Plataforma/Super Admin permanece apenas como FRONTEIRA arquitetural preparada, sem implementar a operacao completa na Fase 1 (coerente com FR-34 fora da entrega operacional).
- (decision) A1/AD-cand: Regras de dependencia entre modulos - (1) nenhum modulo acessa diretamente tabelas/repositorios internos de outro; (2) comunicacao sincrona por contratos de aplicacao explicitos; (3) comunicacao assincrona, quando necessaria, por eventos definidos; (4) entidades de dominio nao dependem de NestJS, Prisma, CASL ou infraestrutura (dominio no centro). Dependencia sempre em direcao ao kernel, nunca o inverso.
- (decision) A1/AD-cand: API interna nao e publica para terceiros na Fase 1, mas EXIGE autenticacao, autorizacao, validacao, isolamento por Organizacao e contratos estaveis.
- (direction) A1/Deferred (decisao posterior): banco unico vs estrategias adicionais de isolamento; contratos e versionamento da API interna; eventos entre modulos; estrutura exata das pastas (seed); politica de deploy independente entre frontend e backend.
- (decision) A2/AD-cand: Isolamento multi-tenant = PostgreSQL COMPARTILHADO; orgId em todo dado organizacional; enforcement de aplicacao + RLS do PostgreSQL; deny-by-default; identificadores estaveis (orgId/phaseId). Binds: todo acesso a dado operacional. Prevents: vazamento entre Organizacoes (NFR-3).
- (decision) A2/AD-cand: Membership multi-organizacao = Forma B (1 usuario : N Organizacoes). Modelar separadamente: Account/User (identidade global da Plataforma), Membership (vinculo usuario<->Organizacao, com papel e estado), activeOrganizationId (contexto operacional atual da sessao/requisicao).
- (decision) A2/AD-cand: orgId NAO fica na conta global do usuario; fica em Membership e em TODOS os dados pertencentes a Organizacao.
- (decision) A2/AD-cand: Toda requisicao valida NO SERVIDOR se o usuario possui Membership ativa na Organizacao solicitada. Nunca confiar somente no orgId enviado pelo frontend. Prevents: acesso cross-tenant por orgId forjado.
- (decision) A2/AD-cand: RLS - o papel usado pela aplicacao NAO pode ter BYPASSRLS nem depender de privilegio de proprietario; usar FORCE ROW LEVEL SECURITY nas tabelas organizacionais; PostgreSQL aplica negacao padrao quando RLS ativo sem politica aplicavel.
- (decision) A2/AD-cand: O contexto de Organizacao usado pelo RLS e definido DENTRO da transacao (set_config(..., true)), nunca globalmente na conexao compartilhada. Prevents: vazamento de contexto entre requisicoes no pool de conexoes.
- (decision) A2/AD-cand: Integridade entre relacoes organizacionais - Card nao referencia Fase de outra Organizacao; Registro nao se vincula a Card de outra Organizacao; chaves unicas locais consideram a Organizacao; relacoes entre dados organizacionais impedem cruzamento de tenant.
- (decision) A2/AD-cand: O contexto de Organizacao ACOMPANHA tambem: filas e jobs, eventos internos, cache, arquivos, notificacoes, auditoria e correlacao de logs. Prevents: vazamento de tenant fora do caminho sincrono de banco.
- (decision) A2/AD-cand: Papeis de banco SEPARADOS - aplicacao comum com RLS obrigatorio; migrations/manutencao com privilegio controlado; nunca usar papel administrativo nas requisicoes normais.
- (direction) A2/Deferred: Prisma pode usar client contextual por requisicao/extensao para apoiar RLS, mas o mecanismo exato fica para implementacao e testes de seguranca.
- (question) A2/Pendencia controlada de UX (feedback cross-stage): a baseline UX (EXPERIENCE.md, final) nao previu seletor de Organizacao (assumiu contexto unico). Como a Forma B foi escolhida, a UX precisara de uma pequena atualizacao (Update) definindo a troca de Organizacao. Regra: com apenas 1 Membership, nenhum seletor aparece. Owner: Produto/UX. Oferecer Update do EXPERIENCE.md.
- (decision) A3/AD-cand: Better Auth para identidade e sessao. Membership e a FONTE OFICIAL do vinculo usuario-Organizacao; evitar uma segunda matriz de permissoes concorrente dentro do Better Auth.
- (decision) A3/AD-cand: Sessao pode guardar activeOrganizationId como contexto MUTAVEL, mas ele nunca e autorizacao suficiente. Troca de Organizacao por operacao validada no servidor; cada requisicao confirma que a Membership continua ativa. Better Auth suporta Org ativa na sessao, mas a validacao de acesso continua obrigatoria.
- (decision) A3/AD-cand: Revogacao - se Membership for removida/suspensa/papel alterado: acesso anterior deixa de valer; abilities em cache sao invalidadas; Organizacao ativa e limpa quando nao mais acessivel; nao manter permissoes duradouras dentro de tokens.
- (decision) A3/AD-cand: CASL - abilities por action + subject + conditions; Membership, papel e atributos do recurso alimentam a politica; Pipe/Card/Database/Registro e demais sao SUBJECTS (nao apenas 4 niveis rigidos); ausencia de regra = negado. CASL nao substitui tenancy, RLS nem validacao no banco.
- (decision) A3/AD-cand: Backend e a AUTORIDADE de autorizacao; frontend adapta UI mas nao concede acesso. AuthZ aplicada tambem em: comandos e consultas, WebSocket, jobs e filas, eventos internos, arquivos, acoes de Automacao e IA.
- (decision) A3/AD-cand: Tipos de principal - usuario autenticado; processo interno/job; Automacao; servico de Plataforma. Todo principal operacional carrega Organizacao, ator/origem e permissoes compativeis.
- (decision) A3/AD-cand: Operacoes sensiveis usam sessao recente/step-up (Better Auth tem verificacao de frescor configuravel); Produto e Seguranca definem exatamente quais acoes exigem step-up.
- (decision) A3/AD-cand: Permissoes de Plataforma NUNCA concedem acesso implicito aos dados de uma Organizacao. Futuro acesso excepcional de suporte exigira fluxo explicito, justificativa, autorizacao e Auditoria (coerente com OQ-39, fora da Fase 1).
- (constraint) A3: Recuperacao de senha - preservar compatibilidade arquitetural, mas NAO tratar a implementacao como aprovada antes da OQ-48 (decisao de Produto).
- (question) A3/Deferred bloqueante-parcial: a matriz de permissoes efetivas (OQ-1..4) pode continuar pendente nesta rodada, mas DEVE ser resolvida antes de fechar contratos detalhados e Stories dos modulos afetados. Owner: Produto.
- (question) A3/Cross-stage UX: atualizar a UX do seletor de Organizacao ANTES de Epicos/Stories; o seletor aparece somente quando o usuario possuir mais de uma Membership ativa. Owner: Produto/UX (Update do EXPERIENCE.md).
- (decision) B1/AD-cand: Propriedade - Plataforma: Conta, Sessao e identidade global. Organizacao: Membership, dados operacionais e Auditoria administrativa. Logs tecnicos: operados pela Plataforma, mas sempre com contexto de Organizacao quando aplicavel.
- (decision) B1/AD-cand: Relatorios - configuracoes de Relatorio pertencem a Organizacao; indicadores calculados/cache sao DADOS DERIVADOS, nunca fonte de verdade.
- (decision) B1/AD-cand: Toda entidade organizacional (INCLUSIVE entidades de associacao) possui contexto de Organizacao. Relacoes validam SIMULTANEAMENTE: existencia do alvo, mesma Organizacao, e autorizacao do principal.
- (decision) B1/AD-cand: Unicidade - quando existir uma regra de unicidade funcional, ela considera a Organizacao, salvo identificadores globais definidos explicitamente.
- (decision) B1/AD-cand: Vinculos com cardinalidade pendente (Card<->Registro OQ-18, Tarefa<->Card OQ-23, E-mail<->Card OQ-29) - identificacao estavel e integridade de tenant sao OBRIGATORIAS; a ESTRUTURA FISICA sera escolhida apos Produto definir a cardinalidade. NAO afirmar que o mecanismo ja suportara 1-N e N-N. Nenhuma relacao deve ser criada apenas para 'preparar o futuro'.
- (decision) B1/AD-cand: Formulario e Campo possuem IDs estaveis; valores de Registro NAO dependem do nome visual do Campo; renomear um Campo nao pode perder nem deslocar valores; remocao/alteracao de Campo deve preservar o historico dos Registros.
- (decision) B1/AD-cand: Acao de Automacao <-> Template por ID estavel com integridade referencial (nao texto livre). Registro e org-owned, ligado a um Database, valores conformam ao Formulario. Catalogo/estrutura de Campo = Produto (OQ-9).
- (direction) B1/Deferred: estrategia de versionamento/evolucao de Formulario/Campo definida antes da implementacao.
- (direction) B1/Deferred: Acao<->Template - ID estavel nao resolve alteracoes posteriores; decidir depois entre: usar sempre a versao atual / fixar uma versao / armazenar snapshot no momento da execucao.
- (direction) B1/Deferred: NAO definir JSONB, colunas dinamicas ou tabelas de valores nesta rodada; decidir apos catalogo de campos (OQ-9), consultas esperadas, indexacao e evolucao dos Formularios. Cardinalidades permanecem de Produto (OQ-18/23/29).
- (decision) B2/AD-cand: Mutacoes pela camada de aplicacao do dominio DONO. Nenhum modulo escreve diretamente no repositorio de outro dominio.
- (decision) B2/AD-cand: Separar evento de dominio (fato interno ocorrido no dominio) de evento de integracao interno (contrato estavel usado por outros modulos).
- (decision) B2/AD-cand (direction): Nao publicar efeitos externos antes da confirmacao da transacao. A alteracao principal e o registro do evento confiavel sao confirmados de forma ATOMICA; o processamento assincrono ocorre depois. Mecanismo concreto (ex.: Outbox) detalhado posteriormente.
- (decision) B2/AD-cand: Eventos e consumidores possuem identificador unico, Organizacao, ator/origem, data/hora, tipo e versao, correlacao, e processamento IDEMPOTENTE.
- (decision) B2/AD-cand: NAO determinar que todo evento gera Historico, Notificacao ou Automacao. Cada consumidor so reage quando o comportamento estiver aprovado pelo Produto.
- (decision) B2/AD-cand: Fonte de verdade - cada conceito tem um dominio proprietario e uma fonte autoritativa; projecoes, caches, indicadores e indices de busca sao DERIVADOS e RECONSTRUIVEIS.
- (decision) B2/AD-cand: Quatro trilhas (Historico do Card, Log operacional, Auditoria administrativa, Log tecnico) - finalidades e acessos distintos, politicas de retencao proprias; nenhuma armazena segredos ou dados pessoais desnecessarios; Auditoria administrativa nao editavel como dado comum, respeitando LGPD e retencao. Log tecnico nunca despejado no Historico do Card.
- (decision) B2/AD-cand: Formularios/Registros preservam - identidade estavel de Campo; VERSAO da definicao utilizada; valores historicos mesmo apos renomear/alterar Campo; estrategia explicita para remocao, transformacao ou descontinuacao de Campos.
- (decision) B2/AD-cand: Concorrencia - alteracoes simultaneas nao podem sobrescrever dados silenciosamente; o mecanismo de controle de concorrencia sera definido por dominio.
- (decision) B2/AD-cand: Regra de alteracao destrutiva/migrations cobre tambem - rollback ou recuperacao; backfill; compatibilidade entre versoes durante deploy; validacao de isolamento por Organizacao. Sem migration code nesta etapa.
- (constraint) B2: Ciclo de publicacao do Formulario (rascunho/salvar/pre-visualizar/publicar/despublicar) e persistencia dos valores de Fase (OQ-21) continuam decisoes de Produto/UX; NAO criar estados tecnicos definitivos antes dessas decisoes.
- (direction) B2/Deferred: mecanismo concreto de projecao de eventos->Historico/Notificacao/Automacao (implementacao); Outbox e controle de concorrencia por dominio (Stories).
- (decision) C1/AD-cand: Automacoes consomem EVENTOS INTERNOS DE INTEGRACAO, nao eventos de dominio diretamente. Publicacao ocorre APOS a transacao principal, preferencialmente via Outbox.
- (decision) C1/AD-cand: A fila pode entregar novamente uma execucao; consumidores e Acoes devem ser IDEMPOTENTES e possuir chave de deduplicacao (nao usar 'efeito nunca duplicado').
- (decision) C1/AD-cand: Ao executar o job, REVALIDAR no servidor: Organizacao, Automacao ativa, versao da Automacao, permissoes do principal, existencia dos recursos referenciados. Nao confiar apenas no payload da fila.
- (decision) C1/AD-cand: Principal Automacao com capacidades EXPLICITAS e deny-by-default; nao herda indefinidamente as permissoes do usuario que a criou.
- (decision) C1/AD-cand: Versionar a definicao da Automacao; uma execucao registra qual versao de Evento, Condicao e Acao utilizou. Decisao futura sobre versao/snapshot de Template (B1 deferred).
- (decision) C1/AD-cand: Falhas registram - retries limitados, backoff, timeout, estado final de falha, possibilidade controlada de reprocessamento, e alerta quando a falha nao puder ser recuperada.
- (decision) C1/AD-cand: Prevencao de ciclos (NFR-7) usa executionChainId, evento causador, profundidade maxima, Automacoes ja visitadas, e deduplicacao por evento/Automacao.
- (decision) C1/AD-cand: IA como Acao - a geracao automatica pode ocorrer; o resultado fica PENDENTE de revisao; somente um evento posterior de APROVACAO pode liberar qualquer efeito operacional (NFR-17). Modo de uso IA-como-Acao vs IA-auxilia-criacao = Produto (OQ-27).
- (decision) C1/AD-cand: Trilha de Execucoes registra ID da execucao, Organizacao, Automacao e versao, evento causador, tentativa, duracao, resultado e erro SANITIZADO - sem segredos nem dados pessoais desnecessarios (NFR-6).
- (direction) C1/Deferred: concorrencia e ordenacao - decisao posterior. Nao prometer ordem global; definir por dominio quando duas execucoes afetarem o mesmo Card ou recurso. Parametros concretos de retry/backoff/timeout/limite de cadeia e mecanismo de idempotencia = implementacao/QA (OQ-25).
- (decision) C2/AD-cand: Socket.IO e canal BEST-EFFORT - indisponibilidade nao perde o estado real; reconexao dispara nova leitura da fonte persistida; o cliente nao presume que recebeu todos os eventos.
- (decision) C2/AD-cand: Ao trocar a Organizacao ativa - cancelar inscricoes da Organizacao anterior; limpar dados contextuais do cliente; validar a nova Membership no servidor; somente entao inscrever nos novos canais.
- (decision) C2/AD-cand: Separar conteudo/evento da Notificacao do ESTADO especifico de entrega e leitura por destinatario, caso o modelo de distribuicao aprovado exija multiplos destinatarios. Nao afirmar ainda que toda Notificacao e 'persistida por destinatario'; semantica final na OQ-33.
- (decision) C2/AD-cand: Notificacoes geradas por EVENTOS INTERNOS DE INTEGRACAO confirmados apos a transacao, com consumidor idempotente e deduplicacao.
- (decision) C2/AD-cand: Badge/popover/pagina usam a mesma fonte autoritativa; popover exibe subconjunto recente; pagina respeita paginacao, retencao e permissoes; contagem de nao lidas e calculada no escopo do usuario e da Organizacao atual (INV-NOTIF-01).
- (decision) C2/AD-cand: Eventos do Socket.IO contem apenas o minimo - tipo, recurso afetado, versao ou momento, correlacao. Evitar conteudo pessoal, segredos ou objetos completos; o cliente busca os dados autorizados pela API.
- (decision) C2/AD-cand: Eventos fora de ordem e duplicados - atualizacoes idempotentes; payload permite detectar versao antiga; o cliente refaz a leitura quando houver duvida de consistencia.
- (decision) C2/AD-cand: Revalidar autorizacao ao conectar, ao assinar canal, ao trocar Organizacao, e apos remocao ou alteracao de Membership/papel.
- (decision) C2/AD-cand: Cache Redis - chaves sempre incluem Organizacao e contexto necessario; nao misturar cache entre tenants; autorizacao nao pode depender de cache antigo; mudancas de papel ou Membership exigem invalidacao; ausencia/falha do cache cai para a fonte autoritativa.
- (decision) C2/AD-cand: Nao usar eventos de dominio diretamente no frontend; o Socket.IO publica CONTRATOS DE ATUALIZACAO DE INTERFACE proprios e versionados.
- (decision) C2/AD-cand: Observabilidade de tempo real - conexoes/reconexoes, assinaturas negadas, falhas de entrega, atraso de eventos, consumidor de Notificacoes, divergencia entre tempo real e fonte persistida; sem registrar conteudo pessoal desnecessario.
- (decision) C3/AD-cand: Portas SEPARADAS por capacidade (nao uma porta generica): envio de E-mail; recebimento/sincronizacao de E-mail; execucao de IA; armazenamento de arquivos. O dominio NAO depende de OpenAI Agents SDK, SDK do provedor de E-mail nem cliente MinIO - esses pertencem aos adapters de infraestrutura.
- (decision) C3/AD-cand: E-mail (se envio real aprovado) - usar fila, idempotencia e chave de deduplicacao; registrar tentativa, resultado e erro sanitizado; limitar retries e timeout; tratar entrega/rejeicao/falha somente quando o Produto definir esses estados.
- (decision) C3/AD-cand: Recebimento de E-mail e capacidade SEPARADA do envio. Callbacks tecnicos do provedor podem existir como detalhe interno, com validacao de assinatura, deduplicacao, identificacao segura da Organizacao e rejeicao de eventos invalidos.
- (decision) C3/AD-cand: IA - o modelo NAO e um usuario com permissoes proprias. A chamada ocorre em nome de um usuario, Automacao ou servico interno explicitamente autorizado (corrige o conceito de principal IA do A3).
- (decision) C3/AD-cand: A saida aceita pela IA NAO executa diretamente o efeito - ela gera um COMANDO SEPARADO, autorizado e auditavel para enviar E-mail, alterar dados ou realizar outra acao (NFR-17).
- (decision) C3/AD-cand: Guardrails tecnicos da IA - minimizacao e possivel mascaramento dos dados; protecao contra prompt injection e vazamento de instrucoes; timeout, retry limitado e circuit breaker; limites de custo/consumo por Organizacao; politica explicita para armazenamento de prompts/respostas; fallback manual.
- (decision) C3/AD-cand: FAIL-CLOSED - se decisoes juridicas ou de Produto nao estiverem aprovadas, E-mail real, IA e arquivos permanecem DESABILITADOS por configuracao e OCULTOS na UX.
- (decision) C3/AD-cand: Storage - metadados e autorizacao ficam na aplicacao/banco; buckets nao publicos; acesso por URL temporaria e curta; validar tamanho, tipo e conteudo; usar checksum; prever quarentena e verificacao de arquivo malicioso; impedir acesso cruzado mesmo conhecendo a chave do objeto.
- (decision) C3/AD-cand: Arquivos possuem ciclo de vida definido - upload incompleto, arquivo orfao, substituicao, exclusao logica/fisica, retencao, restauracao e backup.
- (decision) C3/AD-cand: Credenciais de TODOS os provedores vem de gerenciamento seguro de segredos, nunca de banco comum, frontend, logs ou payloads de fila (NFR-1).
- (decision) C3/AD-cand: Observabilidade de chamadas externas - provedor, Organizacao, correlacao, duracao, tentativas, resultado, consumo/custo quando aplicavel; sem conteudo pessoal ou segredos desnecessarios.
- (direction) C3/Deferred: provedores concretos (e-mail real, config da IA) - stack §14; mecanismo de residencia/regiao de dados quando a avaliacao juridica de transferencia fechar (OQ-44). Gated: OQ-28/30/31/32/47.
- (decision) D1/AD-cand: Observabilidade cobre logs estruturados, erros, metricas, traces/correlacao, health checks e alertas. Pino e Sentry sao ferramentas, nao a definicao completa da capacidade.
- (decision) D1/AD-cand: Campos minimos padronizados em toda observabilidade - servico e ambiente, correlationId, Organizacao, ator/origem, operacao, recurso, resultado, duracao, data/hora.
- (decision) D1/AD-cand: Politica CENTRAL de sanitizacao para Pino e Sentry. Nunca registrar: senhas; tokens, cookies e headers de autenticacao; segredos; corpos completos de E-mail; prompts/respostas integrais de IA; conteudo de arquivos; dados pessoais desnecessarios (NFR-1).
- (decision) D1/AD-cand: As quatro trilhas (Historico, Log operacional, Auditoria, Log tecnico) tem armazenamento, acesso e retencao SEPARADOS. Nenhuma trilha e fonte de verdade do dominio.
- (decision) D1/AD-cand: Auditoria administrativa e APPEND-ONLY e resistente a alteracao pelo fluxo comum, permitindo apenas retencao, anonimizacao ou exclusao controlada por politica legal (INV-AUDIT-01).
- (decision) D1/AD-cand: Um evento de Auditoria contem apenas o necessario - Organizacao, ator, acao, recurso afetado, resultado, data/hora, correlacao. Valores anteriores/posteriores somente quando aprovados e necessarios.
- (decision) D1/AD-cand: Operacoes administrativas sensiveis registram Auditoria de forma CONFIAVEL junto da mutacao (por transacao ou mecanismo equivalente), para evitar acao concluida sem trilha correspondente.
- (decision) D1/AD-cand: Monitoramento e alertas para - tentativas repetidas de autenticacao; acesso negado entre Organizacoes; alteracoes de papel e Membership; revogacao de sessoes; falhas permanentes em jobs/Automacoes; falhas de provedores; erros de isolamento ou autorizacao.
- (decision) D1/AD-cand: Acesso a logs, Sentry e Auditoria segue menor privilegio; acessos administrativos a observabilidade de producao tambem precisam ser rastreaveis.
- (direction) D1/Deferred: retencao e niveis concretos de alerta ficam para D2, mas nenhum log ou evento deve ser mantido INDEFINIDAMENTE por padrao. Catalogo de eventos auditaveis = Produto (OQ-38).
- (decision) D2/AD-cand: Dois cenarios de recuperacao - (1) recuperacao COMPLETA da Plataforma; (2) recuperacao LOGICA de uma Organizacao, somente se tecnicamente suportada. Nao prometer restauracao individual por tenant sem validacao pratica.
- (decision) D2/AD-cand: Banco e arquivos restaurados de forma COERENTE, usando ponto de recuperacao ou manifesto comum. Nao presumir snapshot atomico entre PostgreSQL e MinIO.
- (decision) D2/AD-cand: Backups - criptografados; credenciais separadas; fora do mesmo dominio de falha da producao; nao publicos; retencao definida; respeitar isolamento e LGPD.
- (decision) D2/AD-cand: Backup concluido NAO comprova recuperabilidade. Restores devem ser testados periodicamente, com evidencia do resultado.
- (decision) D2/AD-cand: Deploy inclui health/readiness checks; encerramento gracioso; rollback da aplicacao; monitoramento apos publicacao; migrations executadas como ETAPA CONTROLADA, nao automaticamente por cada container.
- (decision) D2/AD-cand: Migrations - nem toda alteracao destrutiva permite rollback seguro. Toda migration exige estrategia de roll-forward, recuperacao ou restauracao, alem de compatibilidade entre versoes durante o deploy.
- (decision) D2/AD-cand: Ambientes possuem banco, buckets, filas, cache e segredos SEPARADOS. Dados reais de producao nao podem ir para desenvolvimento/testes sem anonimizacao autorizada.
- (decision) D2/AD-cand: Decisao de residencia abrange banco, arquivos, backups, logs/observabilidade, E-mail, IA e subprocessadores (OQ-44).
- (decision) D2/AD-cand: Se CI/CD ainda nao definido, exigir procedimento MANUAL REPRODUZIVEL de deploy, rollback e recuperacao. CI/CD permanece decisao posterior de Engenharia. backup-check/migration-check apenas verificam; controles reais na infra/implementacao/testes/operacao.
- (direction) D2/Deferred: valores de RPO/RTO, politicas de retencao e comportamento de encerramento de Organizacao devem ser decididos ANTES DA PRODUCAO, mesmo que nao bloqueiem o fechamento da Arquitetura (OQ-40/41/42). Deploy conteinerizado via Docker Compose/Coolify, containers distintos, segredos fora do repo.
- (event) spine finalized — Arquitetura Giraffe CRM Fase 1 aprovada formalmente (ARCHITECTURE-SPINE.md status: final, updated 2026-07-11). AD-1..AD-34 distilados (Binds/Prevents/Rule) + Consistency Conventions + Stack (seed, versoes diferidas) + Structural Seed (2 diagramas) + Capability->Architecture Map + Deferred. Reviewer Gate inline APROVADO: cada AD passa no teste do invariante; nenhuma dimensao estrutural silenciosa; separacao Arquitetura x Produto/Juridico preservada (decisoes de Produto/Juridico mantidas em Deferred com dono); escopo do PRD preservado; zero codigo/schema/migrations. Item cross-stage: seletor de Organizacao (Forma B) a adicionar ao EXPERIENCE.md via Update controlado antes de Epicos/Stories. Baseline oficial para bmad-spec / bmad-create-epics-and-stories.

- (decision) 2026-07-17/OQ-47 DECIDIDA pelo dono -> AD-28 DESTRAVADO **apenas para arquivos** (e-mail outbound e IA seguem gated; OQ-32 e OQ-43..46 abertas). Binds: Epico 3 Stories 3.7/3.8/3.10; consumidores E5 (anexos de Tarefa/Solicitacao) e E6 (anexos de e-mail). Prevents: Stories de upload implementadas sem limites numericos (a epics os exigia ANTES das Stories) e sem antivirus nomeado ("verificacao de arquivo malicioso" sem implementacao nomeada e requisito que nenhum teste reprova). Rule: AD-27 NAO muda -- ele ja mandava MinIO, buckets privados, URL curta, validacao, checksum, quarentena, sem acesso cruzado por chave e ciclo de vida; a ADR-001 o INSTANCIA com o que faltava. Detalhe: docs/03-arquitetura/adr-001-capacidade-de-arquivos.md
- (decision) 2026-07-17/AD-27 instanciado: numeros = 10 MiB por arquivo, 10 arquivos por Registro, 1 GiB por tenant, URL assinada <= 60 s. Antivirus = ClamAV obrigatorio. Maquina de estados = QUARANTINED -> AVAILABLE so com veredito CLEAN; erro/timeout do scanner MANTEM quarentena (ausencia de veredito nao e veredito limpo -- e o modo de falha mais provavel, porque acontece sozinho); INFECTED terminal. Quarentena e liberados sao BUCKETS distintos, nao prefixos (prefixo e convencao, bucket e fronteira). Validacao por magic bytes + allowlist positiva, nunca por extensao. Download revalida autorizacao e envia attachment + nosniff (HTML/SVG inline executaria na origem do CRM = XSS armazenado com o cookie de sessao). Sem bypass administrativo, sem storage local, sem entrega antes do scan.
- (decision) 2026-07-17/AD-28 runtime: o destravamento e da DECISAO, nao do runtime. FILE_UPLOAD_ENABLED segue default false; todo limite e o scanner sao fail-closed (ausente/ilegivel => NEGA, nunca "sem limite"). Rollback = FILE_UPLOAD_ENABLED=false, sem migration, reusando o gate ja testado da 2.4 (podePublicarComArquivo).
- (correcao) 2026-07-17/RETIFICA a entrada anterior deste memlog: "Rollback = FILE_UPLOAD_ENABLED=false ... reusando o gate ja testado da 2.4 (podePublicarComArquivo)" e FALSO. Verificado no codigo: `podePublicarComArquivo` (file-gate.ts:24) decide PUBLICABILIDADE DE FORMULARIO e seus unicos chamadores sao snapshot.ts:79 e publication.service.ts:121 -- nao conhece rota, upload nem download. A capacidade precisa de gate PROPRIO. Registrado aqui porque a entrada errada ja estava no memlog e apaga-la seria pior que corrigi-la.
- (bloqueio) 2026-07-17/ADR-001 v2 REPROVADA por duas revisoes read-only independentes (8 CRITICAL, 13 HIGH). Conflitos AUTORITATIVOS abertos, que exigem decisao do dono e workflow oficial: (1) FILE_MAX_TENANT_BYTES esta em "Fora do escopo" da epics 3.7 ("limites por Org/Formulario (fora da Fase 1)"), mas a baseline do dono o exige; (2) a Opcao A REVOGA "acesso por URL temporaria e curta" da Rule do AD-27 e contradiz a letra da epics 3.7 AC#2 -- e emenda, nao instanciacao; (3) o 409 de rollback sobre FormVersion legada exige alterar submissao (2.7/3.4/2.8), fora das dependencias declaradas da 3.7; (4) ClamAV+MinIO estouram o orcamento de 4 GB/2 vCPU que o proprio docker-compose.yml declara para o host coabitado com o Chatwoot. A 3.7 NAO entra em ready-for-dev antes disso.
- (decision) 2026-07-17/ADR-001 v3, decisoes do dono: (1) CHECKSUM = SHA-256 no ingest + SEGUNDO SHA-256 independente, relendo os bytes REAIS do storage no pipeline de scan; so promove se coincidirem (mata a tautologia da v2, em que o hash era comparado consigo mesmo e o AC passava em qualquer sistema). (2) STS por tenant NAO e MVP -- remover todo AC que prometa isolamento imposto pelo storage e REGISTRAR HONESTAMENTE que o backend tem credencial UNICA e que o isolamento vem de autorizacao + RLS + chave construida no servidor + guarda do adapter; storage privado, credencial minima e chave nao fornecida pelo cliente sao controles COMPENSATORIOS; STS vira hardening futuro, nao bloqueador. (3) GUARDA DE PREFIXO testada DIRETO no adapter com spy/fake, provando ZERO interacao com o MinIO -- a integracao HTTP/RLS deixa de ser a prova (na v2 o AC passava com a guarda deletada, porque a RLS matava a requisicao antes do adapter). (4) RATE LIMIT: autorizada a EXTRACAO do primitivo atomico da 2.8 para um kernel antiabuso generico, com adapter compativel preservando a 2.8 sem mudanca de contrato; a 3.7 usa namespace proprio por tenant+ator/recurso, nunca o publicId hardcoded.
- (resolucao) 2026-07-17/Conflitos autoritativos 2, 3 e 4 resolvidos na alcada da Arquitetura: (2) a Opcao A e EMENDA declarada ao AD-27/epics 3.7 AC#2 (nao "cumprimento") -- clausula da URL temporaria suspensa para arquivos, pendente de ratificacao pelo workflow BMAD; (3) o 409 de rollback segue o precedente `podePublicarComArquivo`: a 3.7 DECLARA a constante e a funcao de gate, a 3.8 CONSOME -- assim a 3.7 nao toca pipes/ nem databases/ e suas dependencias declaradas (1.2/1.3/1.4/1.6) ficam intactas; (4) minio/clamav vao SO para o docker-compose.override.yml (dev/CI; o Coolify nao o carrega) -- o compose base declara orcamento de 4 GB/2 vCPU JA NO TETO num host que coabita com o Chatwoot, e um clamd de ~2 GB ali seria risco de OOM sobre sistema que a ordem manda nao tocar. Producao = AD-32/deploy.
- (bloqueio) 2026-07-17/Conflito autoritativo 1 ABERTO, aguardando o dono: FILE_MAX_TENANT_BYTES (1 GiB/tenant) e exigido pela baseline do dono e EXCLUIDO pela epics 3.7 ("Fora do escopo: limites por Org/Formulario (fora da Fase 1)"; tenant = Organizacao por AD-6/AD-10). Constitution II proibe ampliar o escopo da Fase 1. Saidas: (a) emendar a epics pelo workflow BMAD e manter o limite + ACs 11/12; (b) remover o limite por tenant da v3, ficando com FILE_MAX_BYTES + FILE_MAX_PER_RESOURCE (que a epics autoriza literalmente) e registrar a cota por Org como debito de Fase 2. NAO decidido por mim: os dois lados sao do dono.
- (resolucao) 2026-07-17/Conflito autoritativo 1 RESOLVIDO pelo dono: opcao (b). FILE_MAX_TENANT_BYTES REMOVIDO da ADR, dos ACs e das envs; a epics NAO foi emendada (a baseline cedeu ao artefato autoritativo, Constitution II). Fase 1 fica com FILE_MAX_BYTES + FILE_MAX_PER_RESOURCE + rate limit atomico + concorrencia de scan + quarentena/expurgo + monitoramento (que DETECTA, nao protege) + fail-closed. Cota por tenant vira debito DEB-1 pos-MVP com impacto/mitigacao/Story-alvo/responsavel/gatilho (antes de self-service ou cobranca por uso)/gate (concorrencia + contabilizacao de bytes fisicos). RETIFICA a entrada (bloqueio) anterior, que ficou dizendo "ABERTO, aguardando o dono" depois de ja decidido.
- (bloqueio) 2026-07-17/ADR-001 v3 REPROVADA por duas revisoes read-only independentes (Arquitetura: 4 CRITICAL/4 HIGH; Seguranca: 7 HIGH). Reincidencia da MESMA classe que reprovou v1 e v2 -- afirmacao nao verificada e AC que nao pode reprovar. Achados de fundo: (1) a v3 citou a epics 3.7 AC#2 PELA METADE (cortou "URL temporaria, de curta duracao", preservou "vinculada ao usuario/recurso/finalidade") e chamou de CUMPRIMENTO, enquanto o Spine registrava o mesmo ponto como conflito aberto na MESMA data -- e doze secoes adiante a propria v3 exigiu de si o oposto na 6.1 ("a baseline cedeu ao artefato autoritativo"); (2) inventou FILES_ENABLED sem jamais mencionar FILE_UPLOAD_ENABLED, que EXISTE (env.ts:169) e cujo .env.example:91 diz "Deixe vazio ate o E3" -- dois gates para uma capacidade; (3) o modelo de ameaca creditou controles REMOVIDOS na propria v3 (T1 citava a STS adiada; T10 citava a cota removida); (4) "o TOCTOU some junto com o limite que o exigia" e FALSO -- FILE_MAX_PER_RESOURCE E um contador e era racy (10 uploads concorrentes num recurso com 9 => 19 arquivos num limite de 10, com o AC sequencial verde); (5) reteve o nome do arquivo (que ela mesma classifica como PII) INDEFINIDAMENTE em INFECTED/DELETED, seis secoes depois de acusar a v1 de "retencao indefinida contradizendo o proprio T9"; (6) AC de cross-tenant TAUTOLOGICO ("negado pelo banco (RLS+FORCE) e pela RLS" = a mesma camada duas vezes, sem mutacao); (7) 417 linhas sobre isolamento multi-tenant de arquivos SEM dizer tabela, RLS, GRANT ou se o metadado recebe DELETE, numa base onde o GRANT e a fronteira; (8) o Rollback dizia "a 3.7 nao toca pipes/" enquanto a secao Antiabuso mandava extrair codigo de pipes/public-submissions/.
- (decision) 2026-07-17/ADR-001 v4: fecha os achados na alcada da Arquitetura. NOVO: secao 0 "Divergencias autoritativas REGISTRADAS" com DIV-1 (AD-27 "URL temporaria e curta" EMENDADA) e DIV-2 (epics 3.7 AC#2 NAO cumprido na 1a clausula), ambas em BLOCO LITERAL com arquivo:linha e pendentes dos workflows oficiais -- a 3.7 NAO entra em ready-for-dev antes das duas emendas. Regra adotada: toda citacao de artefato autoritativo nesta ADR e bloco literal com arquivo:linha, nunca parafrase nem recorte (o DIV-2 nao teria sobrevivido a ela). NOVA secao 2 (Persistencia): FileObject org-scoped, RLS ENABLE+FORCE, WITH CHECK no INSERT E no UPDATE, MODELOS_AUDITADOS, GRANT SELECT/INSERT + UPDATE column-scoped (state/nomeOriginal/veredito/updatedAt/purgedAt) SEM DELETE -- molde do precedente Record (migrations/20260716180000_records/migration.sql:120-123); bucketKey/sha256/orgId/resourceId sem UPDATE (chave imutavel + SHA forense preservado PELO BANCO); nomeOriginal COM UPDATE porque e o que torna a anonimizacao LGPD executavel sem DELETE. Gate unico = FILE_UPLOAD_ENABLED (FILES_ENABLED eliminado). AC#2 (409 sobre FormVersion legada) movido para a 3.8 -- intestavel na 3.7 por vacuidade, inclusive com a funcao de gate deletada; o AC da 3.7 e a funcao PURA (precedente forms-file-gate.test.ts). Modelo de ameaca so cita controle que existira na Fase 1; T5b (scanner CEGO), T13 (corrupcao scan->promocao) e T15 (auto-DoS por limites do clamd) acrescentados.
- (decision) 2026-07-17/ADR-001 v4, achados de Seguranca fechados: (a) SCANNER CEGO -- clamd com base de assinaturas vazia/obsoleta NAO erra, NAO estoura limite e NAO da timeout: responde "stream: OK" para malware conhecido e nao cai em nenhuma das 5 condicoes de fail-closed; malware ia a AVAILABLE com os ACs 7 e 8 VERDES. Fechado por canario EICAR no boot/periodico (exige INFECTED, senao scannerApto=false => nega) + CLAMAV_DB_MAX_AGE_HOURS, com AC e mutacao (remover o canario => EICAR promovido => vermelho). (b) COERENCIA DE LIMITES -- com AlertExceedsMax yes, clamd com MaxFileSize/StreamMaxLength < FILE_MAX_BYTES marca TODO arquivo legitimo como INFECTED (terminal): perda de dado do titular por ma configuracao; boot valida e falha alto. (c) RATE LIMIT chaveado por tenant+ator permite K membros = K x TETO e por tenant+recurso NUNCA binda -- a chave BINDA em orgId (files:orgId), pois e o controle que substitui a cota removida; sub-chave por ator so como limite mais estreito. (d) SEMAFORO de scan in-process NAO protege (N replicas => N x limite, AD-32): e DISTRIBUIDO no banco, com slots que EXPIRAM (replica morta trancaria a Org fora da capacidade -- fail-closed viraria self-DoS permanente). (e) FILE_MAX_PER_RESOURCE atomico (pg_advisory_xact_lock na mesma tx interativa, ou guarda otimista) + AC CONCORRENTE com mutacao. (f) PII: nomeOriginal ANONIMIZADO no ato do expurgo dos bytes; SHA-256 + veredito retidos por FILE_FORENSIC_RETENTION_DAYS (90) -- para a forense o hash basta, o nome nao. (g) allowlist ENUMERADA (PDF/PNG/JPEG/GIF/WEBP), SVG REJEITADO na base (epics.md:1229 exige "rejeitar SVG ativo"). (h) download fixa Content-Type = tipo DETECTADO pelos magic bytes (nunca o declarado) + CSP default-src none; sandbox. (i) rate limit e limite por recurso viram PASSOS 0 e 2 da ordem obrigatoria do upload -- antes de qualquer byte aceito (precedente public-rate-limit.ts:17: "a checagem precede qualquer escrita"); a v3 os tinha fora do pipeline normativo, e 10 MiB ja teriam sido recebidos por requisicao negada. (j) promocao = CopyObject condicional ao checksum (if-match): os dois SHA cobriam ingest->scan e deixavam scan->promocao aberta. (k) AC dos dois SHA ganha costura via StoragePort decorador de teste -- sem ela o AC era INEXECUTAVEL (scan sincrono no mesmo handler) e a "solucao" natural seria comparar dois hashes do mesmo buffer, que E o defeito da v2. (l) guarda de prefixo por SEGMENTO (split barra [0] === orgId), nao startsWith -- um startsWith ingenuo passava no AC como escrito. (m) RateLimit e tabela GLOBAL sem RLS (schema.prisma:263) acessada no client raiz: declarado como excecao deliberada (infra de plataforma, como Account/PublicFormRoute -- AD-10), senao um implementador tentaria envolve-la em withTenantContext (que recusa $transaction) ou lhe daria RLS e QUEBRARIA a 2.8. (n) kernel/antiabuso expoe SO o primitivo contar(chave,janela,teto); janela e teto ficam nos dominios chamadores -- move-los para o kernel seria regra de negocio no kernel, violando o AD-4 que a propria ADR invoca.
- (bloqueio) 2026-07-17/TRES decisoes do dono PENDENTES na ADR-001 v4, registradas como Q1/Q2/Q3 e NAO decididas por mim: Q1 -- epics.md:1175 diz "tamanho max por arquivo e limite total por recurso"; "limite total" e soma de BYTES ou CONTAGEM de arquivos? v1-v3 leram contagem (10 arquivos), mas pareado com "tamanho max POR ARQUIVO" le-se como bytes, e a leitura por contagem permite 10 x 10 MiB = 100 MiB por recurso -- exatamente o buraco deixado pela remocao da cota por tenant; recomendacao: adotar OS DOIS (FILE_MAX_PER_RESOURCE + FILE_MAX_BYTES_PER_RESOURCE), ambos cabem na frase e ambos sao "por recurso", que a epics autoriza. Q2 -- epics.md:1193 poe "rate limit e protecao contra abuso" no escopo da 3.8, nao da 3.7; a extracao do primitivo + adapter + refatoracao da 2.8 e trabalho sobre o Epico 2 e contradiz "a 3.7 nao toca pipes/"; recomendacao: extracao e rate limit viram TECH STORY propria (precedente tech-d01-hop-web-api-autenticado.md) da qual a 3.7 depende, e o semaforo de concorrencia de scan FICA na 3.7 (protege o scanner que a 3.7 introduz). Q3 -- text/plain, text/csv e application/json NAO tem magic bytes, e a ADR torna magic bytes obrigatorio; inclui-los torna a regra inimplementavel, exclui-los proibe anexar .txt/.csv; documentos Office sao ZIP (PK) com risco de macro. Fail-closed no interim: o que nao esta na allowlist e negado.
- (correcao) 2026-07-17/RETIFICA duas edicoes MINHAS no ARCHITECTURE-SPINE.md: (1) eu havia subido `updated: 2026-07-11` -> `2026-07-17` num artefato status: final -- exatamente o bump de data que o dono ja rejeitara antes e pedira para nunca repetir; revertido para 2026-07-11 e o Spine volta a mudar so no seu fluxo proprio. (2) a nota do AD-28 dizia "arquivos -- DESTRAVADO" na MESMA pagina em que a nota do AD-27 dizia "NAO IMPLEMENTAR AINDA" e com a ADR em PROPOSTA; corrigida para "a DECISAO de Produto existe; o gate NAO esta destravado -- destravar exige as duas emendas (DIV-1/DIV-2) + security-check".
- (resolucao) 2026-07-17/DIV-1 e DIV-2 EMENDADAS pelo workflow oficial bmad-correct-course (Sprint Change Proposal sprint-change-proposal-2026-07-17.md). DIV-1: AD-27 Rule "acesso por URL temporaria e curta" -> "acesso por entrega autenticada sob a sessao do usuario (proxy/stream pela API)", texto original PRESERVADO por marcacao de emenda inline (nao exclusao), frontmatter updated/status INALTERADOS (convencao do projeto). DIV-2: epics 3.7 AC#2 "URL temporaria, de curta duracao" -> "entrega autenticada sob a sessao do usuario (stream pela API)", mantendo "vinculada ao usuario/recurso/finalidade" e "sem link publico permanente", original preservado inline. As notas AD-27/AD-28 do Spine reconciliadas: divergencias RESOLVIDAS, falta so merge da ADR + security-check; ressalva LOW fechada (FileScan citado no resumo). Escopo Minor (ajuste de redacao), sem replan.
- (decision) 2026-07-17/Q1/Q2/Q3 do dono aplicadas na ADR v5. Q1: FILE_MAX_PER_RESOURCE = 10 arquivos/recurso (CONTAGEM, nao bytes; recomendacao "os dois" REVOGADA; sem FILE_MAX_BYTES_PER_RESOURCE na Fase 1; cota agregada por bytes segue como DEB-1). Q2: rate limit + semaforo de scan pertencem a 3.7 ("a capacidade nunca entra no ar desprotegida"); a EXTRACAO do primitivo antiabuso e TECH STORY pre-requisito da 3.7 (nao trabalho embutido -> a 3.7 nao toca pipes/, consome o kernel ja extraido); a 3.8 acrescenta so limites do consumidor/canal publico. Q3: .txt/.csv/.json FORA da allowlist inicial; NAO enfraquecer o gate de magic bytes; entram no futuro so com validacao UTF-8 + parser + limites estruturais + testes.
- (decision) 2026-07-17/ADR-001 v5 fecha as ressalvas da 3a revisao (APROVADA COM RESSALVAS da v4): MEDIUM = persistencia do semaforo especificada -> tabela GLOBAL de plataforma ScanSlot (sem RLS, como RateLimit/Account/PublicFormRoute; chave scan:<orgId>; colunas key/token/expiraEm; GRANT SELECT/INSERT/DELETE porque liberarSlot apaga a linha; vive em kernel/antiabuso/), com adquirirSlot (statement atomico conta slots ativos e insere so abaixo do teto -> token ou null/429) e liberarSlot (DELETE where key+token, em finally). A RateLimit NAO e reusada (nao tem token/expiraEm). LOW = FileScan citado na nota do Spine. Status da ADR = RATIFICADA (design) — em re-revisao final do delta v5; gates de runtime da 3.7 (pre-implementation/security/migration/lgpd-check) seguem obrigatorios no momento do codigo, implementacao NAO autorizada ainda.

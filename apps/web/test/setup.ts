// Matchers do jest-dom para os testes de componente (toHaveAttribute, toBeInTheDocument, ...).
// Registrar aqui é inócuo para os testes de lógica pura — só adiciona matchers ao `expect`.
import '@testing-library/jest-dom/vitest';
